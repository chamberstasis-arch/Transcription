# El frontend se sirve same-origin (backend en producción, proxy de Vite en dev),
# así que usa rutas relativas y NO necesita configurar la URL de la API.

# Solo para desarrollo con Vite: destino al que se proxyan /api y /app.
VITE_PROXY_TARGET=http://localhost:8000
